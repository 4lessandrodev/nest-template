import { Field, Int, ObjectType } from '@nestjs/graphql';

@ObjectType()
export class {{pascalCase name}}Type {
	@Field(() => Int)
	id!: number;

	@Field(() => String)
	name!: string;
}

export default {{pascalCase name}}Type;
